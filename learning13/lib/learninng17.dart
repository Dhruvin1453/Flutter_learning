import 'package:flutter/material.dart';
import 'package:font_awesome_flutter/font_awesome_flutter.dart';

class learninng17 extends StatelessWidget{
  @override
  Widget build(BuildContext context) {

    return Scaffold(

      // Awsome Icones In Flutter
      //First we go in google and search font awsome flutter
      //then we got latest dependency the copy and set dependency
      // also copy import class line and import class


      body: Center(child: FaIcon(FontAwesomeIcons.apple  5
        ,size: 30,)),

    );

  }



}